//! ============================================================================
//! flag —— 配置读取/解析与命令行入口(仿 Go 侧 flag 包)
//! ============================================================================
//!
//! 职责:
//! - 从工作目录 ./config.yaml 读取配置;缺失时用内置默认(include_str!
//!   编译期嵌入 config.yaml)落盘生成后继续;
//! - serde 反序列化 + 必填校验,配置缺失/非法 → 返回错误(启动即终止);
//! - 共享密钥经环境变量注入(禁止写入配置文件)。
//!
//! 引用方式:`crate::flag::enter::{load_config, ...}`。
pub mod enter;
