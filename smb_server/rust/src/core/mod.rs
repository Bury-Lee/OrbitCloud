//! ============================================================================
//! core —— 全局配置与运行时单例(仿 Go 侧 core 包:只放全局单例/配置,
//! 不 import 上层业务模块)。
//! ============================================================================
//!
//! 分层约定(与 Go 侧一致):
//! ```text
//! main(入口) → flag(配置读取/解析) → core(全局配置/单例)
//!                            ↘ types(帧协议契约) → remote_backend / sync
//! ```
//!
//! 本模块只定义数据与访问入口,不承载业务逻辑。
//! 引用方式:`crate::core::enter::{Config, ...}`。

pub mod enter;
